<Page
    x:Class="HTMLRenderer.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HTMLRenderer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:common="using:html2xaml"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <StackPanel >
        <TextBox TextWrapping="Wrap"
                 Width="400"
                 AcceptsReturn="True"
                 x:Name="Html"></TextBox>
        <Button x:Name="btn" Click="btn_Click" HorizontalAlignment="Center">Parse</Button>
        <RichTextBlock
            Height="400"
            common:Properties.Html="{x:Bind Html.Text,Mode=OneWay}"
            ></RichTextBlock>
    </StackPanel>
</Page>
